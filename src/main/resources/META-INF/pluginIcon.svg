<svg xmlns="http://www.w3.org/2000/svg"
     viewBox="0 0 40 40">
  <!-- Outer circle -->
  <circle cx="20" cy="20" r="18"
          fill="none"
          stroke="currentColor"
          stroke-width="2"/>

  <!-- Play / Run triangle -->
  <polygon points="17,14 27,20 17,26"
           fill="currentColor"/>

  <!-- Workflow dots -->
  <circle cx="13" cy="20" r="2" fill="currentColor"/>
  <circle cx="30" cy="20" r="2" fill="currentColor"/>
</svg>
